{% for blk in blocks %}
// =============================================================================
// UVM Test Suite for Register Map Verification
// Compliant with IEEE 1800.2 (uvm-ieee)
// Generated by rmap - Hardware Register Map Designer
// =============================================================================

`timescale 1ns / 1ps

// =============================================================================
// 1. Base Test
// =============================================================================
class {{ lower(blk.name) }}_base_test extends uvm_test;
  `uvm_component_utils({{ lower(blk.name) }}_base_test)

  {{ lower(blk.name) }}_reg_env env;

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  virtual function void build_phase(uvm_phase phase);
    super.build_phase(phase);
    env = {{ lower(blk.name) }}_reg_env::type_id::create("env", this);
  endfunction

  virtual function void end_of_elaboration_phase(uvm_phase phase);
    super.end_of_elaboration_phase(phase);
    uvm_top.print_topology();
  endfunction
endclass : {{ lower(blk.name) }}_base_test


// =============================================================================
// 2. IEEE 1800.2 Standard HW Reset Test (uvm_reg_hw_reset_seq)
// =============================================================================
class {{ lower(blk.name) }}_hw_reset_test extends {{ lower(blk.name) }}_base_test;
  `uvm_component_utils({{ lower(blk.name) }}_hw_reset_test)

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  virtual task run_phase(uvm_phase phase);
    uvm_reg_hw_reset_seq seq;
    phase.raise_objection(this);

    `uvm_info("TEST", "Starting IEEE 1800.2 uvm_reg_hw_reset_seq...", UVM_LOW)
    seq = uvm_reg_hw_reset_seq::type_id::create("seq");
    seq.model = env.reg_model;
    seq.start(null);

    `uvm_info("TEST", "Completed IEEE 1800.2 uvm_reg_hw_reset_seq successfully!", UVM_LOW)
    phase.drop_objection(this);
  endtask
endclass : {{ lower(blk.name) }}_hw_reset_test


// =============================================================================
// 3. IEEE 1800.2 Standard Bit Bash Test (uvm_reg_bit_bash_seq)
// =============================================================================
class {{ lower(blk.name) }}_bit_bash_test extends {{ lower(blk.name) }}_base_test;
  `uvm_component_utils({{ lower(blk.name) }}_bit_bash_test)

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  virtual task run_phase(uvm_phase phase);
    uvm_reg_bit_bash_seq seq;
    phase.raise_objection(this);

    `uvm_info("TEST", "Starting IEEE 1800.2 uvm_reg_bit_bash_seq...", UVM_LOW)
    seq = uvm_reg_bit_bash_seq::type_id::create("seq");
    seq.model = env.reg_model;
    seq.start(null);

    `uvm_info("TEST", "Completed IEEE 1800.2 uvm_reg_bit_bash_seq successfully!", UVM_LOW)
    phase.drop_objection(this);
  endtask
endclass : {{ lower(blk.name) }}_bit_bash_test


// =============================================================================
// 4. IEEE 1800.2 Standard Register Access Test (uvm_reg_access_seq)
// =============================================================================
class {{ lower(blk.name) }}_reg_access_test extends {{ lower(blk.name) }}_base_test;
  `uvm_component_utils({{ lower(blk.name) }}_reg_access_test)

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  virtual task run_phase(uvm_phase phase);
    uvm_reg_access_seq seq;
    phase.raise_objection(this);

    `uvm_info("TEST", "Starting IEEE 1800.2 uvm_reg_access_seq...", UVM_LOW)
    seq = uvm_reg_access_seq::type_id::create("seq");
    seq.model = env.reg_model;
    seq.start(null);

    `uvm_info("TEST", "Completed IEEE 1800.2 uvm_reg_access_seq successfully!", UVM_LOW)
    phase.drop_objection(this);
  endtask
endclass : {{ lower(blk.name) }}_reg_access_test
{% if existsIn(blk, "memories") and length(blk.memories) > 0 %}


// =============================================================================
// 5. IEEE 1800.2 Standard Memory Walk Test (uvm_mem_walk_seq)
// =============================================================================
class {{ lower(blk.name) }}_mem_walk_test extends {{ lower(blk.name) }}_base_test;
  `uvm_component_utils({{ lower(blk.name) }}_mem_walk_test)

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  virtual task run_phase(uvm_phase phase);
    uvm_mem_walk_seq seq;
    phase.raise_objection(this);

    `uvm_info("TEST", "Starting IEEE 1800.2 uvm_mem_walk_seq...", UVM_LOW)
    seq = uvm_mem_walk_seq::type_id::create("seq");
    seq.model = env.reg_model;
    seq.start(null);

    `uvm_info("TEST", "Completed IEEE 1800.2 uvm_mem_walk_seq successfully!", UVM_LOW)
    phase.drop_objection(this);
  endtask
endclass : {{ lower(blk.name) }}_mem_walk_test


// =============================================================================
// 6. IEEE 1800.2 Standard Memory Access Test (uvm_mem_access_seq)
// =============================================================================
class {{ lower(blk.name) }}_mem_access_test extends {{ lower(blk.name) }}_base_test;
  `uvm_component_utils({{ lower(blk.name) }}_mem_access_test)

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  virtual task run_phase(uvm_phase phase);
    uvm_mem_access_seq seq;
    phase.raise_objection(this);

    `uvm_info("TEST", "Starting IEEE 1800.2 uvm_mem_access_seq...", UVM_LOW)
    seq = uvm_mem_access_seq::type_id::create("seq");
    seq.model = env.reg_model;
    seq.start(null);

    `uvm_info("TEST", "Completed IEEE 1800.2 uvm_mem_access_seq successfully!", UVM_LOW)
    phase.drop_objection(this);
  endtask
endclass : {{ lower(blk.name) }}_mem_access_test
{% endif %}
{% endfor %}
